"""
INTEL CONFIDENTIAL

Copyright (C) 2023 Intel Corporation. All Rights Reserved.

The source code contained or described herein and all documents
related to the source code ("Material") are owned by Intel Corporation
or licensors. Title to the Material remains with Intel
Corporation or its licensors. The Material contains trade
secrets and proprietary and confidential information of Intel or its
licensors. The Material is protected by worldwide copyright
and trade secret laws and treaty provisions. No part of the Material may
be used, copied, reproduced, modified, published, uploaded, posted,
transmitted, distributed, or disclosed in any way without Intel's prior
express written permission.

No License under any patent, copyright, trade secret or other intellectual
property right is granted to or conferred upon you by disclosure or
delivery of the Materials, either expressly, by implication, inducement,
estoppel or otherwise. Any license under such intellectual property rights
must be express and approved by Intel in writing.
"""

module(name = "vila", version = "1.0.1")

bazel_dep(name = "bazel_skylib", version = "1.7.1")
bazel_dep(name = "bazel_features", version = "1.17.0")
bazel_dep(name = "rules_cc", version = "0.0.10")
bazel_dep(name = "rules_python", version = "0.38.0")
bazel_dep(name = "rules_foreign_cc", version = "0.12.0")
bazel_dep(name = "googletest", version = "1.15.2", repo_name = "com_google_googletest")
bazel_dep(name = "google_benchmark", version = "1.8.5", repo_name = "com_google_benchmark")
bazel_dep(name = "pybind11_bazel", version = "2.13.6")
bazel_dep(name = "fmt", version = "11.0.2")
# bazel_dep(name = "abseil-cpp", version = "20240116.2")

bazel_dep(name = "platforms", version = "0.0.10")  # python config depends platforms
py_config_ext = use_extension("@vila//vila/bazel/bzlmod:extensions.bzl", "python_configure_extension")
use_repo(py_config_ext, "local_config_python")

wdk_config_ext = use_extension("@vila//vila/bazel/bzlmod:extensions.bzl", "wdk_configure_extension")
use_repo(wdk_config_ext, "local_config_wdk")

bullseye_config_ext = use_extension("@vila//vila/bazel/bzlmod:extensions.bzl", "bullseye_configure_extension")
use_repo(bullseye_config_ext, "local_config_bullseye")

# register_toolchains("@local_config_bullseye//:cc-toolchain-x64_windows")

spdlog_ext = use_extension("@vila//vila/bazel/bzlmod:extensions.bzl", "spdlog_extension")
use_repo(spdlog_ext, "spdlog")

rangev3_ext = use_extension("@vila//vila/bazel/bzlmod:extensions.bzl", "rangev3_extension")
use_repo(rangev3_ext, "rangev3")

hedron_ext = use_extension("@vila//vila/bazel/bzlmod:extensions.bzl", "hedron_extension")
use_repo(hedron_ext, "hedron_compile_commands")

icpx_config_ext = use_extension("@vila//vila/bazel/bzlmod:extensions.bzl", "icpx_configure_extension")
use_repo(icpx_config_ext, "local_config_icpx")

# Comment out the following line to use the icpx compiler on Windows
# register_toolchains("@local_config_icpx//:cc-toolchain-x64_windows")
